# RUN MAP — Autonomous sprint 2026-07-12 (Greg at work)

This is a CONTRACT, not a draft. It converts `KICKOFF-v5-DRAFT.md`'s
non-gated candidates + the register-locked v6 items
(`HORIZON-v20.md` §Register outcomes) into an unattended run. The
executing session follows this document; anything not IN SCOPE below is
out, full stop.

## 0. Authorization & operating conditions

- **Greg cannot interact.** He is at work. Do not ask questions. If a
  decision needs him, take the conservative branch, record it in the
  handoff, and move on.
- **Deploy IS authorized for this run.** Greg, this session
  (2026-07-12): "return a /goal prompt for it to finish all non gated
  work and deploy when done after a review since i cant deploy from
  work." This overrides the standing deploys-Greg-run doctrine for THIS
  run only, and only after the codex review + full gate are green.
- **Greg will test from his phone before heading home.** The deployed
  board must be verified live before the wrap-up push — his phone test
  is the real acceptance.
- **Wrap-up notification is mandatory** (§7) — success or blocked, he
  gets a Bark push either way.

## 1. Verified starting state (2026-07-12)

- `war-room/v3` local == origin @ `882d59b`; tree clean except untracked
  `.planning/v2/LOOP-LOCK` (leave it).
- Deployed NEXUS = `16d7eee` (`ssh nexus-ts 'curl -s
http://127.0.0.1:3141/api/version'`). Re-verify before starting.
- Gate on deployed code: server 966 · webview-v3 455 · webview-ui 294 ·
  poller 190 · e2e 31 · build/types/lint clean.
- Estate audit 2026-07-12: ALL GREEN
  (`.planning/v4/INFRA-AUDIT-2026-07-12.md`). Two accepted findings are
  Phase D work items below.

## 2. Phases (each: implement → full gate → codex review → reconcile → commit)

Phases are independent and resumable; a failed phase gets reverted (or
its commits left un-deployed) and recorded, never half-shipped. Order
below is dependency order.

### Phase A — Gate integrity (C5, S)

- Repoint the 4 stale standalone e2e specs (budget-pause,
  disappearing-view, hud-layout ×2) — `button[title=…]` selectors died
  when ControlTooltip absorbed title attrs. If a spec targets the
  retiring old face, note it for C4 (do NOT delete the face — gated).
- Add webview-v3 to root `check-types`.
- **Exit:** standalone smoke green in one pass, no 120s retry burn; a
  seeded webview-v3 type error fails root `npm run check-types`
  (prove, then remove the seed).

### Phase B — Hygiene + addressing hardening (C9 + C8, M)

- C9 (verify-first — some pieces may already exist, e.g.
  `createBudgetPauseNotifier` is built; check its wiring before writing
  code): durable STOP-ALL server latch; `budget-paused` Bark
  edge-trigger wired; drawer `since=` anchor fix; nonce/answerRequests
  LRU/TTL bounds.
- C8: server-side focus validation against advertised `focus:true`;
  answer/kill/focus keyed on (machine, pid, startTime) or sessionId —
  pid-reuse fix.
- **Exit:** the acceptance line per item in KICKOFF-v5-DRAFT C8/C9,
  each proven by a test.

### Phase C — Districts build-out (C1, M)

- Step 1 (ops): fresh read-only TWE + war-room checkouts on nexus
  (refresh-stale-clones is a pre-approved auto-rung class, Q18);
  uncomment the runbook mount block (`d7fa85f` unwired it
  deliberately). Mount changes take effect at the Phase-F deploy.
- Step 2 (code): N-project world layout, per-project buildings from
  real STATE.md milestone state. Honest ⊘ NO DATA for anything the
  parser can't derive — never plausible-but-stale.
- **Exit (post-deploy, checked in Phase F):** `/api/districts` returns
  ≥4 projects with real milestone-derived floors and zero
  `source:unknown`; phone view renders them.

### Phase D — Infra fixes locked by the register (Q44, S)

- **WAR_ROOM_TOKEN out of plist env** (MACBOOK runner launchd plist):
  move to a 0600 file the runner reads (or equivalent) so `launchctl
print` no longer shows it. Receipts + a written undo path BEFORE
  touching the plist; restart the runner and verify it polls green.
  Never echo the token — masked always.
- **MINI becomes a real clone + ship-check**: replace the rsync copy
  per the audit report; add/verify `ship-to-mini.sh` drift check.
  MINI is reachable at 100.121.189.6 (the `mini` ssh alias is stale).
- **Exit:** both runners poll green on the live board after the
  changes; token invisible in `launchctl print`; MINI clone matches
  origin sha.

### Phase E — Documents (no code, S)

- **C3 design doc** (design ONLY — the build stays gated): born-managed
  wrapper + full free-form remote prompting (direction settled by Q17).
  Containment: runner-owned tmux, no pty injection (rejected class),
  one-shot nonce + receipts like T2, and the open questions the draft
  names (scrollback/resize/exit codes, opt-in vs opt-out default →
  Greg's gate). Write to `.planning/v5/C3-DESIGN.md`.
- **Adversarial worst-case audit** (Q43): read-only; what's the worst
  the system can do with access it already HAS, is each path receipted
  and undoable. Write to `.planning/v5/ADVERSARIAL-AUDIT-2026-07-12.md`.
- **Exit:** both docs committed; zero code changes in this phase.

### Phase F — Review, deploy, verify, notify

1. Full gate re-derived at HEAD (all suites + build + types + lint).
2. codex cross-model review of the whole sprint diff
   (`git diff 882d59b..HEAD`); reconcile majors before deploy —
   P5/P6 both caught real pre-ship bugs, this step is not optional.
3. Push branch. Deploy: `cd /Users/greg/code/war-room &&
NEXUS_HOST=nexus-ts bash .planning/runbooks/nexus-war-room-deploy.sh -y`.
   **If the permission layer denies the runbook** (it denied it once on
   2026-07-12): do NOT force it — leave everything pushed + gated,
   send the BLOCKED push (§7) including the one-liner for Greg.
4. Live verify: `/api/version` == new sha via ssh loopback AND
   `https://nexus.tail722a2e.ts.net:8484` reachable; spot-check
   `/api/districts` (Phase C exit) and both runners green.
5. Send the wrap-up push (§7), write the handoff (§8).

## 3. GATED — do not touch (Greg-only)

- **C4 old-face deletion** — gated on Greg's on-phone PWA acceptance
  (he tests TODAY after work; still pending as of this writing).
  `webview-ui/` stays, its 294-test lane stays.
- **C2 sprite/asset pass** — burns codex/ChatGPT budget 3-5x per
  image; recorded preference is attended or /loop-batched. Not for an
  unattended run.
- **C6 per-machine tokens** — a rotation misstep bricks runners while
  Greg can't recover them. Deferred to an attended session (this is a
  deliberate call, recorded here).
- **C7 on-device tuning, C10 Mini sessions decision** — need Greg
  physically.
- **C3 BUILD** — design doc only (Phase E); build after Greg's design
  gate.
- **Morning-page absorption (register Q3)** — one broken morning kills
  the habit (Q49); never cut over Greg's working morning unattended.
- **Vault routine PR-gate flip (Q10)** — vault policy change, Greg
  approves the mechanism first.
- **Funnel** — never. Tailnet-only serve, forever.

## 4. Standing doctrine (unchanged, binding)

- Explicit-path staging + STAGED_COUNT guard before every commit;
  never `git add -A`/`.`. Commit subjects: conventional prefix +
  em-dash. Trailer: `Co-Authored-By: Claude Fable 5
<noreply@anthropic.com>` (or the executing model's own trailer).
- Colorblind shape+word (`✓ ✗ ⚠ ◷ ⊘`) primary in every output.
- Honest data: ⊘ NO DATA over plausible zeros, everywhere.
- Secrets masked always (WAR_ROOM_TOKEN, Bark URLs with keys).
- Verify-first: re-derive claimed state from live commands before
  building on it; read state back after changing it.
- Worktree caution: check for parallel sessions/stale locks before
  staging. If spawning lanes for THIS repo from a session rooted
  elsewhere, lanes must `git worktree add` the war-room repo
  themselves (recorded lesson — agent isolation keys off the session
  repo).

## 5. Failure posture

- A phase that can't pass its gate: revert or leave un-deployed,
  record in the handoff, continue with the next phase. Never deploy a
  red gate. Never deploy without the codex review.
- If NEXUS deploy succeeds but live verify fails: run the runbook's
  rollback (redeploy `16d7eee`), verify the old face of the board is
  back, and send the BLOCKED push with what happened.

## 6. Success criteria (the whole run)

- Phases A–E committed with green gates; codex majors reconciled.
- New sha deployed and live-verified (or cleanly BLOCKED with Greg's
  one-liner in the push).
- Greg's phone test finds: board up, districts real, runners green,
  nothing about his morning flow changed.

## 7. Wrap-up notification (mandatory, both outcomes)

The NEXUS Bark wrapper takes JSON `{task, status, message}` at
`http://localhost:8581/notify` (on nexus). Send exactly one:

Success:

```bash
ssh nexus-ts "curl -s -X POST http://localhost:8581/notify -H 'Content-Type: application/json' -d '{\"task\":\"War Room Sprint\",\"status\":\"info\",\"message\":\"Sprint complete + deployed <sha>. Phases A-E done, codex-reviewed. Test: https://nexus.tail722a2e.ts.net:8484\"}'"
```

Blocked (deploy denied or a gate failed):

```bash
ssh nexus-ts "curl -s -X POST http://localhost:8581/notify -H 'Content-Type: application/json' -d '{\"task\":\"War Room Sprint\",\"status\":\"warning\",\"message\":\"Sprint <what-happened>. Deploy pending: run the one-liner in SESSION-HANDOFF-2026-07-12-sprint.md\"}'"
```

## 8. Handoff (mandatory)

`SESSION-HANDOFF-2026-07-12-sprint.md` in repo root, per the
nexus-handoff format (verified state from live commands, accomplished
w/ commits, deferred/gated w/ unblock conditions, decisions, max 3
next steps, kickoff prompt). Commit it.
