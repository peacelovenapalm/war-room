# RUN MAP — v5 remainder → v6 → v7 (authored 2026-07-12, Greg at desk)

Execution contract for a fresh session. Read this whole file plus
`.planning/v5/HORIZON-v20.md`, `.planning/v5/HORIZON-QUESTIONS.md`
§ANSWERS, `.planning/v5/C3-BUILD-PLAN.md`, and
`.planning/v5/CROSS-SYSTEM-IDEAS.md` (#1) before any work. Ledger
lives at `.planning/v6/SPRINT-STATE.md` (create from the v5 template
on first touch; update on EVERY phase transition — restart/compaction
resumes from it).

Process note (register Q48): Greg's answer was "v6+ planning happens
fully on the board." THIS document was written off-board at Greg's
explicit direction 2026-07-12 ("lets write out v6 and v7") — the
dogfood obligation transfers to EXECUTION: track this run's phases as
board-visible work (dispatched/receipted where the plane supports it),
and note in the handoff how much of the run was actually followable
from the phone.

## 0. Authorizations granted by Greg 2026-07-12 (this run only)

Recorded verbatim from his four gate answers at the MacBook:

1. **Vault PR-gate: STAGED FLIP.** v7 builds direct vault writes into a
   staging area (`_inbox`) with full audit trail from day one;
   auto-promote to direct in-place vault writes only after **7
   consecutive days with zero bad writes** (bad = denylist violation,
   malformed frontmatter, contradiction-flagged-then-confirmed-wrong,
   or Greg reverting one). Promotion itself must be receipted and
   Bark-announced, and instantly revocable back to staged.
2. **Deploys: END OF EACH VERSION, three total.** One NEXUS deploy each
   after v5-remainder, v6, and v7 close — each ONLY when that
   version's full gate is green + codex-reviewed, followed by live
   verification + a Bark push. `NEXUS_HOST=nexus-ts bash
.planning/runbooks/nexus-war-room-deploy.sh -y`. No other deploys.
3. **Morning cutover: PARALLEL-RUN THEN RETIRE.** Old nexus-notifier
   morning page stays fully alive during shakedown. The old surface
   retires only after **5 consecutive clean board-mornings** (clean =
   push arrived on schedule, board morning surface loaded, data honest
   — no ⊘ where real data existed, no stale values presented as
   fresh). Track the streak honestly in the ledger; a broken morning
   resets it. Retirement is a config change with an undo note, not a
   deletion.
4. **Scope: C2 SPRITES INCLUDED, held for review.** Attempt the sprite
   diversity pass loop-batched; results are staged on a branch /
   preview artifact for Greg's review, NEVER shipped to the live board
   without his verdict. Phone-test gates (districts acceptance, C4)
   are pinged via Bark; C4 old-face deletion proceeds ONLY on Greg's
   explicit reply.

Standing rules that still bind: never echo WAR_ROOM_TOKEN or Bark keys
(mask always); explicit-path staging + STAGED_COUNT guard, never
`git add -A`; per-phase codex cross-model review non-optional;
colorblind shape+word symbols; honest ⊘ NO DATA over fabricated
values; Funnel NEVER (tailnet-only); commit trailer
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## 1. Verify-first (before any work)

- `git log --oneline -3` — expect HEAD at/after `fdefb5a` on
  `war-room/v3`, clean tree (untracked `.planning/v2/LOOP-LOCK` OK).
- `ssh nexus-ts 'curl -s http://127.0.0.1:3141/api/version'` — expect
  `c841054…` (the 2026-07-12 sprint deploy) unless a later deploy
  superseded it; reconcile against this file if not.
- `/api/districts` returns 7 projects, zero `source:'unknown'`.
- Both runners fresh in `/api/dispatch/machines` (`lastSeenAt`).

## 2. Phase V5R — close out v5

### V5R-1 Districts overlap fix

Diagnosis (inferred 2026-07-12, verify before fixing):
`webview-v3/src/engine/districtScene.ts` `PLOT_SPACING = 2` gives rows
~32 screen-px of vertical separation while buildings reach ~56 px
(4 floors × `FLOOR_HEIGHT_PX 14` + roof) — with 7 projects = 3 rows,
front rows occlude the row behind and plaques collide. Greg's phone
report: "the view overlaps them and displays poorly."
Fix: scale row spacing to building height (or raise PLOT_SPACING /
separate X vs Y spacing), keep the phone-portrait tall-not-wide rule
(`MAX_PLOTS_PER_ROW 3`), keep `districtSceneBounds` and nearest-wins
hit-testing consistent with the new spacing. Verify with a rendered
screenshot at 7 projects AND at 2 (no regression for small counts).
_Exit:_ screenshot shows no building/plaque overlap at 7 projects;
hit-test unit coverage still green.

### V5R-2 C3 build (born-managed wrapper + PROMPT)

Execute `.planning/v5/C3-BUILD-PLAN.md` T1–T7 exactly. All gates are
CLOSED (§1 of that plan): mechanism (d), shared `verb` wire shape,
opt-in only, `launchedVia` yes. Zero diff in `dispatch-runner.mjs` /
`dispatch-rules.mjs` — any diff there is a plan violation. Ship
runners to MACBOOK + MINI before first PROMPT use (MINI via bundle;
`scripts/mini-drift-check.sh` verifies).
_Exit:_ C3-BUILD-PLAN §3 pass/fail lines all ✓, design §5 acceptance
criteria checked live.

### V5R-3 C2 sprite diversity (held-for-review, authorization §0.4)

Loop-batched sprite/asset pass per `KICKOFF-v5-DRAFT.md`'s C2 entry.
Stage results (branch + rendered preview grid Greg can eyeball from
the phone); Bark him the preview. DO NOT merge/ship without his
verdict — if no reply by phase end, park as draft and move on.
_Exit:_ preview delivered + verdict recorded, or honestly parked.

### V5R-4 Phone acceptance + C4 (Greg-in-the-loop)

After V5R-1 deploys (see below): Bark Greg to phone-test districts +
overall board. On his explicit "accepted": execute C4 old-face
retirement per the v5 draft (delete `/v1/` grace mount + legacy face,
update the 4 legacy-redirected e2e specs — each spec's comment says
port-or-delete). On "rejected" or silence: C4 stays untouched, record
in ledger.
_Exit:_ Greg's verdict recorded verbatim; C4 done or explicitly parked.

**V5R gate + deploy:** full repo gate (check-types incl. webview-v3,
lint, all unit suites, e2e incl. standalone), codex cross-model review
of the phase diff, reconcile, THEN deploy #1 (§0.2), live-verify
(version sha, districts, runners, tailnet 200, funnel clean), Bark.
Note: V5R-4's phone test needs deploy #1 first — sequence V5R-1/2/3 →
gate → deploy #1 → V5R-4 → (C4 changes, if accepted, ride deploy #2).

## 3. Phase V6 — one glance, one push (AMBIENT rung 1)

Register-locked scope (Q1-3, 6-7, 18, 20, 40-41, 45, 49). Design each
item honestly; where a design choice is genuinely Greg's, prefer the
smallest reversible default and record it in the ledger rather than
stalling.

### V6-1 Board morning surface

The board ABSORBS the morning page (Q3): a morning view on the v3 face
holding what `scripts/nexus-notifier/gen_morning_page.py` +
`morning_page/morning.json` provide today PLUS board state — overnight
summary, ⚠ NEEDS-INPUT count + names, ◷ held-budget jobs, ✉ open
routine PRs (wires verified in CROSS-SYSTEM-IDEAS #1: `/api/ops/review`,
`/api/inbox`, `/api/agents/answers`). ONE pre-triaged Bark push at the
existing notifier cron time opens THIS surface (Q1). Explicit "all
calm" state when nothing needs him (Q7) — a real rendered state, not
silence. Event-driven pushes stay ≤3-5/day (Q2).

### V6-2 NEEDS-YOU count (Q6)

Lock-screen-legible signal: the push (and app badge if the PWA
supports it) carries the needs-you COUNT. Shape+number, never
color-only.

### V6-3 Board announces its own sickness (Q49/Q40)

Degraded-state pushes: if the morning push cannot be generated, or the
board's data sources are stale/unreachable, Greg gets a push SAYING SO
(honest-sick beats silent-broken). Latency bar: seconds fine, minutes
not — instrument and surface load/data-age. Offline actions PARK as
clearable drafts, never auto-fire (Q41).

### V6-4 Autonomy rungs, pre-approved classes (Q18/Q20)

Implement the four pre-approved self-heal action classes: restart dead
runners/daemons, refresh stale clones/mirrors, mechanical vault fixes,
re-run failed routines. EVERY action: receipted (verbatim
receipt + undo path), board-visible, revocable, and subordinate to the
STOP-ALL latch. Board may spawn scheduled + reactive standing orders —
every spawn receipted. No fifth class, no generalization.

### V6-5 Cross-model spot checks (Q45)

Narrative reports (morning summary, phase summaries) get a codex
spot-check lane — sampled, not blocking, discrepancies filed honestly.

### V6-6 Parallel-run bookkeeping (§0.3)

Old morning page untouched and alive. Add the clean-morning streak
counter (ledger or board), define "clean" exactly as §0.3, and the
retirement config-flip prepared but NOT executed until 5-streak —
which will almost certainly land after this run ends; hand the streak
state to the handoff.

**V6 gate + deploy:** same discipline as V5R → deploy #2 → live-verify
(including one real or simulated morning-push round trip) → Bark.
_Version exit (falsifiable bet):_ the morning is ONE push → ONE
surface; count of surfaces Greg must open is measurably down (record
the before/after list).

## 4. Phase V7 — the estate starts remembering (MEMORY rung 1)

Register-locked scope (Q8-14). Vault write authorization is §0.1
(STAGED) — the RUN-MAP-2026-07-12 §3 "vault PR-gate flip" gate is
superseded by §0.1 for exactly this staged design, nothing broader.

### V7-1 Post-session distillation

Every war-room-visible session distills into vault knowledge on
session end (graphify-style routine; reuse the existing `graphify`
skill's conventions where they fit). Skip-tag opt-out honored (Q9).
Scope: whole vault / whole life MINUS the hard denylist — finances/
accounts, health/struggles, named people (Q14) — enforced in code at
the write path, not by convention.

### V7-2 Staged write path (§0.1)

Writes land in `_inbox` staging with full audit trail (who/when/which
session/receipt). Auto-promotion logic per §0.1 (7 clean days),
receipted + Bark-announced + revocable. The vault's dual-push backup
chain must be verified live before the FIRST write.

### V7-3 Decision receipts + recall

Decisions get citable receipts (session, date, verbatim line). The
SEARCH prop answers "what did we decide about X, in which session" —
answer FIRST, receipts on tap (Q8). Staleness decay ◷ on old
knowledge + contradiction flags (Q11); wrong knowledge dies by decay,
not silent deletion.

### V7-4 Act I exit instrumentation

Build the honest measurement for the Act I exit question: surfaces
opened per morning (from V6-6's streak data) and re-derivation
incidents ("we answered X from the graph" vs "we re-derived X"). A
counter that starts at zero is fine — the point is it EXISTS.

**V7 gate + deploy:** same discipline → deploy #3 → live-verify → Bark
wrap-up for the whole run.

## 5. GATED — do not touch (unchanged from v5 run map except §0)

C6 per-machine tokens; C7 tuning + C10 Mini sessions (Greg physically);
WAR_ROOM_TOKEN rotation (attended); opt-out `claude` shadowing;
mechanism (c); economy items (Q33-35 dormant); anything v8+; Funnel
NEVER. C4 and C2 are conditionally in-scope per §0.4 ONLY as written
there. Production DB migrations: per-migration approval, always.

## 6. Anti-stall + honesty discipline

Same as RUN-MAP-2026-07-12 §4b: never wait on Greg to proceed with
non-gated work (park and continue); ledger updated on every
transition; first error only, fix, next; epistemics labeled
(verified/reported/inferred); if genuinely blocked → warning Bark +
handoff anyway. Bark commands: copy the exact §7 command shapes from
`.planning/v5/RUN-MAP-2026-07-12.md` (never inline the key into any
other file or output).

## 7. Wrap-up

Handoff `SESSION-HANDOFF-<date>-v5v7.md` via the nexus-handoff skill:
verified state, per-version accomplishments w/ commits, C2 verdict +
C4 outcome, morning-streak state, staged-write audit state, deferred/
gated, max 3 next steps, kickoff prompt. Also: prune stale
`.claude/worktrees/agent-*` worktrees once Greg's phone test passes.

## 8. Kickoff prompt (hand this to the fresh session verbatim)

```
Read /Users/greg/code/war-room/.planning/v6/RUN-MAP-v5-v7-2026-07-12.md
and execute it end to end as a contract. Verify live state first (its
§1). Greg's four authorizations are in §0 — staged vault flip,
end-of-version deploys x3, parallel morning cutover, C2 held-for-review
— nothing broader. Phases: V5R (districts fix, C3 build per
.planning/v5/C3-BUILD-PLAN.md, C2 preview, phone-gated C4) → V6
(ambient rung 1) → V7 (memory rung 1). Each phase: full gate + codex
cross-model review + reconcile + commit; deploy only at version
boundaries per §0.2 with live verification + Bark. Greg is reachable
via Bark for the two phone gates (districts acceptance, C4) — ping and
continue other work while waiting; never stall. GATED list in §5 is
absolute. Ledger: .planning/v6/SPRINT-STATE.md on every transition.
Wrap up per §7.
```
