# STATE v1.1 — run ledger (machine-updated, newest log entries first)

status: IN PROGRESS <!-- IN PROGRESS | RUN COMPLETE -->

## Items

| #     | Item                                   | Status           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | -------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | Crisis-card click                      | done             | commit bca9ffc; e2e/tests/standalone/triage.spec.ts PASS (1/1); before/after + grayscale screenshots in test-results/e2e/triage-crisis-card-\*.png                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2     | Disappearing view (2a+2b)              | done             | commit a44ede6; e2e/tests/standalone/disappearing-view.spec.ts PASS (12s); unit webview-ui/test/pixiApp.test.ts (4 tests); screenshots test-results/e2e/disappearing-view-0{0-5}-\*.png                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3     | Worker session kill + adversarial pass | done             | commit 24cffbb; server 536/536 (+24), webview 277/277 (+9), bin/poller 85/85 (+11); e2e/tests/standalone/kill.spec.ts PASS (1/1); 4-state screenshots + grayscale in test-results/e2e/kill-\*.png; adversarial panel (4 independent reviewers) all PASS, see log                                                                                                                                                                                                                                                                                                                                      |
| 4     | HUD/overlay layout pass                | done             | commit 5937a48; e2e/tests/standalone/hud-layout.spec.ts PASS (2/2, desktop+mobile); full e2e regression 8/8; screenshots test-results/e2e/hud-layout-{desktop,mobile}-0{0-3}-\*.png                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 5     | Budget-pause visibility                | done             | commit 28ad33b; server 537/537 (+1), webview 282/282 (+5); e2e/tests/standalone/budget-pause.spec.ts PASS (2/2); real end-to-end chain-pause screenshots + grayscale in test-results/e2e/chain-{running,paused}\*.png, constructed-state standing-orders screenshot in test-results/e2e/standing-orders-paused-reasons\*.png                                                                                                                                                                                                                                                                          |
| 6     | Employee-quit persist/broadcast        | done             | commit 5e26214; server 538/538 (+1); new regression test asserts onChange broadcast fires + fresh store instance re-reading the file sees the quit                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 7     | Bark payload fix                       | done             | commit 3688f91; server 540/540 (+2); unit tests assert actual JSON body shape per class; real NEXUS e2e push still gated on Deploy Gate 2/3 (blocked, see log)                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 8     | Help/discoverability                   | done             | commit bc186a3; server 540/540, webview 282/282, poller 85/85 (unchanged — webview-only item); e2e/tests/standalone/help-discoverability.spec.ts PASS (2/2, desktop+mobile); screenshots + grayscale in test-results/e2e/help-discoverability-\*.png; all 3 pieces shipped, no budget-cut fallback needed                                                                                                                                                                                                                                                                                             |
| 9     | FOCUS/osascript                        | review-on-return | root cause (b) confirmed, not (a) — see TUNING.md "[KICKOFF v1.1 item 9]" entry. Reproduced real stderr twice (a real headless `claude` CLI pid AND a real GUI Terminal.app pid both hang ~60s → identical "AppleEvent timed out (-1712)"), ruling out the pid-identity theory. Confirmed the real runner is a launchd LaunchAgent (`com.war-room.dispatch-runner.plist`, RunAtLoad+KeepAlive, no bound TTY) — no interactive surface exists to grant first-run Automation/TCC consent for System Events. No code fix possible; exact System Settings path + ready-to-run diagnostic logged for Greg. |
| 10-13 | F1-F4 (tail)                           | pending          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## Deploy gates

| Gate | After                                          | Status                                                                                |
| ---- | ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| 1    | items 1-2                                      | review-on-return (harness permission gate — see log)                                  |
| 2    | items 3-5                                      | review-on-return (same harness permission gate as gate 1 — not re-attempted, see log) |
| 3    | items 6-9 settled + landed F items + full gate | pending                                                                               |

## Log

<!-- one line per event: ISO time — what happened / what's next -->

- 2026-07-09T08:51:22Z — LOOP-LOCK claimed (pid 97056), first iteration, STATE-v1.1.md created from KICKOFF-v1.1.md template. Next: baseline re-derivation + preflight.
- 2026-07-09T08:56:00Z — baseline re-derivation MATCHES expected exactly: check-types clean, lint clean, server 512/512, webview 264/264, bin/poller 74/74, build clean (PWA output present, 11 precache entries). No mismatch, no stop needed.
- 2026-07-09T08:58:00Z — preflight PASS: `ssh nexus-ts true` reachable; `npx playwright --version` → 1.59.1 present; `git push --dry-run` has no upstream on war-room/v1 (pre-existing — v0/v1 were never pushed to origin; deploy runbook rsyncs the local checkout over SSH directly, no git push in the deploy path, so this is not a blocker). Proceeding to item 1.
- 2026-07-09T09:40:00Z — item 1 DONE (commit bca9ffc). TriagePanel.tsx threads onOpenAgent (App.tsx handleClick) through TriageRowView; row onClick + CLEAR stopPropagation; testHooks.setCrisis added (deterministic e2e trigger, same pattern as existing selectAgent hook) since ch.crisis is server-broadcast-only. New e2e/tests/standalone/triage.spec.ts: mocks a PermissionRequest crisis, clicks the row, asserts `AGENT #<id>` drawer + drawer-row visible — PASS (1/1, 4s once VS Code cached). Before/after + grayscale screenshots captured and visually verified. First e2e run also primed the VS Code test-binary cache (.vscode-test/, 272MB one-time download, ~20s) — de-risks items 2/4/8's own Playwright PASS checks. check-types/lint/test (512/264/74) all re-verified clean post-change. Next: item 2 (disappearing view).
- 2026-07-09T09:42:00Z — item 2 delegated to a build sub-agent (2a run-once mount effect + 2b unified resize ownership, one pass, per KICKOFF spec); orchestrator did read-only item-3 architecture prep in parallel (no concurrent checkout mutation).
- 2026-07-09T10:20:00Z — item 2 DONE (commit a44ede6), independently re-verified by the orchestrator (not trusted on the sub-agent's self-report alone): read the full diff (OfficeCanvas.tsx run-once effect + latest-ref indirection, pixiApp.ts resolution:dpr+autoDensity+init counter, App.tsx dead editorTick-prop removal) — correct and sound. Re-ran check-types/lint clean; full suite server 512/512, webview 268/268 (+4 new pixiApp tests), poller 74/74. Re-ran both e2e/tests/standalone/\*.spec.ts myself (triage.spec.ts regression still green 2.4s; new disappearing-view.spec.ts 12.2s) — viewed 3 of the 12 screenshots directly (initial/zoom/resize), confirmed real rendered office content throughout, no blank frames. Pan-alone investigated and did NOT reproduce (e2e covers it) — no TUNING.md entry needed, clean negative. Items 1-2 both settled → Deploy Gate 1 fires next.
- 2026-07-09T10:22:00Z — Deploy Gate 1 BLOCKED, not by the KICKOFF's own pre-authorization (which does cover this) but by the harness's own permission classifier: `NEXUS_HOST=nexus-ts bash .planning/runbooks/nexus-war-room-deploy.sh -y` was denied — reason given: "the deploy script is invoked with -y to bypass its own confirmation prompt against the nexus host with no preview/dry-run step shown... the generic 'never ask Greg' loop directive does not name skipping that specific confirmation." This is a real-time tool-permission gate, not a KICKOFF scope question — a written doc pre-authorization can't satisfy it; it needs either Greg's live approval or a pre-configured Bash permission rule. Per the classifier's own instruction ("stop and explain, let the user decide") and KICKOFF's blocked-item policy (note-and-continue, don't stall the line), NOT retried, NOT worked around (e.g. piping a fake "deploy" answer to the interactive prompt would defeat the same guard) — logged here + TUNING.md, gate 1 stays pending as `review-on-return`, continuing to item 3. Gates 2 and 3 will hit this identical wall — flagging now so it isn't rediscovered three times. Re-attempt next time Greg is actually present to click through the live approval.
- 2026-07-09T11:10:00Z — item 3 DONE (worker session kill, HIGH-STAKES). Built: (1) runner live-children registry (bin/dispatch-runner.mjs state.children, populated in runDispatch, cleared on real 'exit'); (2) server->runner stop-instruction channel (dispatchStore.ts StopInstruction/stopQueue/drainStopsFor, poll response now `{pending, stop}`); (3) observed-pid kill path (verifyClaudeProcess via real `ps`, deny-by-default, never registry-shortcut); (4) server kill routes (POST /api/dispatch/:id/kill, POST/GET /api/agents/kill[/:id], POST /api/pid-kills/:id/status, extended POST /api/dispatch/:id/status with event:'killed') + chainStore.haltRun (single-run halt, distinct from STOP-ALL's haltAllRunning) + chainOrchestrator 'killed' branch; (5) AgentDrawer KILL button, two-step confirm (StopAllControl pattern), 3 visible states (enabled/CONFIRM/disabled+reason), polls /api/agents/kill/:id for the async outcome. Wire schema: core/asyncapi.yaml DispatchStatusValue/ChainStepStatusValue += 'killed', regenerated messages.ts. Amended .planning/DISPATCH-6B-DESIGN.md (first server->runner imperative). Found + fixed a REAL pre-existing bug along the way: testHooks.ts's `addAgent` monkey-patch only forwarded 6 of 11 params (silently dropped machine/provider/sessionId/cwd/pid in e2e mode only) — fixed via rest-param forwarding; this was blocking the new e2e test from ever seeing a pid/machine-tagged agent. Mandatory adversarial panel (4 independent Sonnet reviewers, distinct lenses, run via the Agent tool since no Workflow tool exists in this session): (a) spoofing/cross-machine — PASS WITH CONCERNS (one real, cheap fix applied: `/api/agents/kill`'s body `machine` field now runs through `sanitizeMachineLabel`, closing a silent-stranding gap; the shared-Bearer-token trust model is pre-existing, not a regression); (b) registry containment — PASS (one documented heuristic limitation: `verifyClaudeProcess`'s `ps`-based check is a substring/word-boundary match over the full command line, not strict binary-identity verification — matches KICKOFF's own "name/cmdline check" wording, not tightened further this round); (c) STOP-ALL regression — PASS, no concerns; (d) safety-net untouched — PASS, standingOrderStore.ts/budgetStore.ts/bin/lib/dispatch-rules.mjs all zero-diff, confirmed via `git diff --stat`. Evidence: server 536/536 (+24: dispatchStore.test.ts stop/pid-kill tests, chainStore.test.ts haltRun tests, chainOrchestrator.test.ts killed-halts-run tests, new killRoutes.test.ts), webview 277/277 (+9: canKillAgent/machineHasLiveRunner/killed-chip tests), bin/poller 85/85 (+11: registry lifecycle, stop-by-id killed/not-found, pid-kill deny/success, verifyClaudeProcess incl. 2 REAL-process tests, END-TO-END real-SIGTERM-kills-a-real-process test verified via `process.kill(pid,0)` probe — caught and fixed a real bug here too: the original E2E test spawned `bash <script>` with inherited stdio pipes, orphaning a `sleep 300` grandchild on SIGTERM and hanging the WHOLE poller suite for exactly 300s; fixed via `bash -c 'exec -a NAME sleep N'` + `stdio:'ignore'`, no separate child process at all). check-types/lint clean (root+server+webview-ui, webview-ui type-checked separately since root tsconfig excludes it). e2e/tests/standalone/kill.spec.ts PASS (1/1, 4.5s): all 3 button states screenshotted incl. grayscale (test-results/e2e/kill-{disabled-before,enabled,confirm,killed}{,-grayscale}.png, viewed directly, all legible shape+word), plus real HTTP round-trip through the actual kill routes (not mocked). Full e2e regression (triage/disappearing-view/hooks/kill, 4 tests) all PASS. Full build clean (11 precache entries). PASS criteria from KICKOFF fully met: two-concurrent-dispatches kill isolation (bin/test + server/killRoutes.test.ts), real observed-session kill with `ps`-verified death, unknown-pid UI-disabled state (no server/runner path built, by design), STOP-ALL still works after an individual kill (chainOrchestrator.test.ts + killRoutes.test.ts). Nothing deferred. Item 3 settled → contributes to Deploy Gate 2 (items 3-5), which stays pending until items 4-5 also settle (gate 2 itself will hit the same harness deploy-permission wall gate 1 hit — not re-attempted here, same reasoning as the gate-1 log entry above).
- 2026-07-09T11:35:00Z — item 3 independently re-verified by the orchestrator (highest-stakes item this run, did not trust the sub-agent's self-report alone): read the full diff for the containment-critical files (bin/dispatch-runner.mjs's verifyClaudeProcess/processDispatchStop/processPidKillStop, server/src/dispatchStore.ts's requestStop/requestPidKill/drainStopsFor, server/src/httpServer.ts's kill routes, server/src/chainStore.ts's haltRun, webview-ui/src/components/AgentDrawer.tsx's kill button) — traced every kill/process.kill call site (exactly two, both correctly gated), confirmed requestStop derives its target machine from the server-stored dispatch record (not caller-supplied, closing the cross-machine-spoofing question for that path); confirmed requestPidKill's caller-supplied machine field is a real but pre-existing, already-flagged trust-model characteristic (matches STOP-ALL's own unauthenticated-tailnet posture, not a new regression) rather than a novel hole. Re-ran check-types/lint clean; full suite server 536/536, webview 277/277, poller 85/85 (confirmed the poller suite completes fast now, no repeat of the previously-fixed 300s hang). Rebuilt webview+CLI fresh and re-ran the full e2e suite myself (4/4 pass: triage/disappearing-view/hooks/kill). Viewed all 3 kill-button-state screenshots + the killed-terminal grayscale copy directly — legible shape+word throughout. Independently re-drove the single highest-risk PASS scenario myself: ran bin/test/dispatch-runner.test.mjs directly, confirmed the END-TO-END real-process-kill test (spawns a real process, real `ps`-based verification, real SIGTERM, death confirmed via `process.kill(pid,0)` existence probe, no mocks) passes on its own. Fixed one small STATE.md evidence gap (agent had left "commit TBD" — filled in the actual SHA, 24cffbb). No further findings. Item 3 confirmed done.
- 2026-07-09T11:47:00Z — item 4 delegated to a build sub-agent (one z-index scale + one corner-stacking helper, migrate every overlay, capped scope per KICKOFF). Orchestrator did read-only item-5 prep in parallel (no concurrent checkout mutation).
- 2026-07-09T13:05:00Z — item 4 taken over and finished by the orchestrator directly. The sub-agent did substantial, genuinely good work (hudLayout.ts HUD_LAYER scale + HudStack.tsx corner-stacking component, migrated 14 components, found and fixed 4 real cross-corner collisions via measured Playwright geometry — BottomToolbar/VersionIndicator, ProgressionHUD/StopAllControl at mobile, TriagePanel mobile overflow, a pointer-events phantom-hitbox issue) but stopped responding mid-way through its last fix iteration (multiple status checks over ~1h got decreasing/no replies despite real progress each time; per my own stated fallback after the last unanswered check, took over rather than wait indefinitely). Found the working tree at a clean, buildable, mostly-passing state: desktop hud-layout.spec.ts PASSED, mobile FAILED on one remaining real collision (`edit-action-bar` vs `triage-panel` when edit-mode-dirty and an active crisis coincide at 390px — the sub-agent had correctly diagnosed this as "same root cause, same fix pattern" in its last message before going unresponsive). Fixed it myself: EditActionBar's 4-button row wraps to a fixed-width 2x2 grid at `max-sm:`, clearing TriagePanel's reserved column. Rebuilt, re-ran the test — both desktop and mobile PASS. Deleted the sub-agent's own acknowledged-temporary `_scratch-hud-probe.spec.ts` (it said it would delete this once done, never got there). Re-ran full check-types/lint/test (277/536/85, unchanged) and the full 8-test e2e regression (triage/disappearing-view/hooks/kill/hud-layout×2/scratch-probe-before-deletion) — all green. Viewed the mobile edit-dirty screenshot directly: visually clean, no overlap, all text legible. Spot-checked the largest diff (ZoomControls.tsx's split into ZoomControls+ZoomLevelBadge) and App.tsx's HudStack wiring (5 corners, correct layer assignments) — sound. Committed as 5937a48. Item 4 settled.
- 2026-07-09T13:45:00Z — item 5 (budget-pause visibility) built by a BUILD sub-agent, reporting here directly (orchestrator's own independent re-verification still pending, not claimed done by this entry alone). Confirmed baseline first (check-types/lint clean, server 536/536, webview 277/277, poller 85/85 — matched item-4 post counts exactly). Threaded budgetStore.ts's existing 4-reason `AutomationPauseResult` (already had stale-snapshot/5h-threshold/7d-threshold/codex-cap-reached — nothing new needed there) through the two places that were discarding it: `chainOrchestrator.ts`'s `IsAutomationPaused` type changed from `() => boolean` to `() => {paused, reason?}` (narrow local duplicate, same one-way-layering convention as `EmployeeDispatchDefaults`); `standingOrderStore.ts` got the same treatment. `httpServer.ts`'s two adapter closures (chain gate + standing-order tick) now pass the full result instead of destructuring `.paused` immediately. `chainStore.ts` gained `ChainRun.pausedReason` + a `setPausedReason()` method (no-op-guarded on unchanged reason so `sweep()`'s per-tick retries of an ongoing pause don't thrash persistence/broadcast) — set on `enqueueStep`'s budget-gated bare-return, cleared right before a step actually enqueues. `standingOrderStore.tick` now records the real reason in `lastSkipReason` instead of the `'budget-paused'` literal (kept a legacy-literal rendering fallback client-side for any already-persisted old value). Webview: added a shared `budget.ts` reason→word map (`isAutomationPauseReason`/`budgetPauseReasonWord`), consumed by both `chainRunChipLabel` (chain.ts) and `standingOrderStatusLabel` (standingOrders.ts) — e.g. `⏸ PAUSED — 5h budget (step 2/3)` vs `⏸ PAUSED — codex cap`, glyph+word per colorblind Hard Rule 3, never touching the existing `◎` running glyph's meaning. Did NOT build a manual resume, did NOT touch the 4 threshold values or the stale-snapshot window, did NOT touch `sweep()`'s auto-retry semantics — visibility only, per KICKOFF scope. Fixed 3 pre-existing test call sites that broke on the signature change (chainOrchestrator.test.ts, standingOrderStore.test.ts, standingOrderStoreRoutes.test.ts) plus one test whose expectation needed updating (webview-ui/test/standingOrders.test.ts's old 'budget-paused' → '⏸ BUDGET PAUSED' assertion, now '⏸ PAUSED — budget' for the legacy-literal fallback) — all genuine signature-follows, not weakened assertions; the standingOrderStoreRoutes.test.ts real-poller-forwarded-95%-snapshot test now asserts the REAL derived reason (`5h-threshold`) instead of the old generic literal, a strictly stronger check. Added net-new coverage: chainStore.test.ts's setPausedReason no-op-guard test, chainOrchestrator.test.ts's budget-gate test now asserts `run.pausedReason` set/cleared, webview-ui's chainBuilder.test.ts + standingOrders.test.ts each got an "all 4 reasons render distinctly" test (Set-size assertion, not just substring checks). PASS evidence: server 537/537 (+1), webview 282/282 (+5), poller 85/85 (unchanged, no poller-side changes this item), check-types/lint clean throughout. New `e2e/tests/standalone/budget-pause.spec.ts` (2/2 PASS): test 1 drives a REAL 2-step chain end-to-end through the actual dispatch lifecycle (POST /api/chains/defs → /run → real accept+exited on step 1's real dispatch via the same Bearer-authed routes bin/dispatch-runner.mjs itself calls) — no mocked budgetStore state anywhere; the standalone fixture never posts a Claude rate-limit snapshot, so budgetStore's own fail-safe default (`stale-snapshot`) fires for real the instant step 2 is attempted, proving the exact production code path with no live Claude Code session reporting in. Screenshots (+ grayscale) captured and visually verified directly: `chain-running.png` reads "CHAIN — ◎ RUNNING (step 1/2)", `chain-paused.png` reads "CHAIN — ⏸ PAUSED — stale telemetry (step 2/2)" — legible, distinct, shape+word both present. Test 2 is a component-level constructed-state screenshot (network response for GET /api/standing-orders mocked via Playwright route interception, real StandingOrdersPanel component rendered) showing all 4 reasons simultaneously and distinctly ("⏸ PAUSED — stale telemetry" / "5h budget" / "7d budget" / "codex cap") — used deliberately instead of waiting out the real 60s standingOrderTick cadence end-to-end, per KICKOFF's own explicit sanction for this exact tradeoff; the real-reason plumbing into `lastSkipReason` is independently proven by test 1's server-side sibling (standingOrderStoreRoutes.test.ts) and by the dedicated unit tests, so nothing here relies on the mock for correctness, only for the screenshot. Committed as 28ad33b (14 files, one commit per this run's established one-commit-per-item convention). Nothing deferred, no TUNING.md entry needed — this item's build fit cleanly inside the visibility-only cap with no forced compromises. Orchestrator re-verification still outstanding.
- 2026-07-09T13:50:00Z — item 5 independently re-verified by the orchestrator: read the full diff (chainOrchestrator.ts's IsAutomationPaused type change + fail-safe-default preserved as `{paused:true, reason:'gate-not-configured'}`, chainStore.ts's setPausedReason no-op guard, httpServer.ts's two adapter sites now pass the full result through, standingOrderStore.ts's real-reason threading with a sane `?? 'budget-paused'` fallback, webview budget.ts's shared reason-word map with forward-compat fallback for unknown reasons, chain.ts/standingOrders.ts's rendering including a deliberate legacy-literal compat path) — sound throughout, no shortcuts. Re-ran check-types/lint clean; full suite server 537/537, webview 282/282, poller 85/85 (all matching the sub-agent's reported counts exactly). Rebuilt and re-ran the full 8-test e2e regression myself — all pass. Viewed chain-paused.png ("CHAIN — ⏸ PAUSED — stale telemetry (step 2/2)"), chain-running.png, and standing-orders-paused-reasons.png (all 4 reasons legible and distinct) plus the grayscale copy of the latter — colorblind rule holds. No further findings. Item 5 confirmed done. Items 3, 4, 5 all settled → Deploy Gate 2 (items 3-5) is now fully settled at the item level; it will hit the same harness deploy-permission wall Gate 1 hit if attempted (not re-attempted here, logged already). Next: item 6 (employee-quit persist/broadcast).
- 2026-07-09T13:52:00Z — Deploy Gate 2 marked review-on-return WITHOUT re-attempting the runbook: the block at gate 1 was a harness-level permission-classifier policy (blind -y bypass of a live-host confirmation), not state that changes between attempts — retrying the identical denied command would just get denied again (and my own instructions say not to re-attempt an identical denied tool call). Same resolution path as gate 1: needs Greg's live approval or a pre-configured Bash permission rule.
- 2026-07-09T14:00:00Z — item 6 DONE, implemented directly by the orchestrator (small enough to skip sub-agent delegation): `employeeStore.ts`'s `applyUpkeep()` quit branch called `appendLedger()` directly instead of `finish()` (persist+ledger+broadcast) like every other mutation — fixed by swapping in `this.finish(emp, now, 'quit', {level, mood})`, identical ledger entry shape, now also persists + broadcasts. New regression test (deterministic seeded quit, same pattern as the existing "halves XP" test) asserts `onChange` fires with the quit snapshot AND a fresh `EmployeeStore` instance re-reading the same file sees `status:'quit'` — proving persist actually wrote it. Hit and fixed one test-authoring bug of my own along the way (not a product bug): my first attempt only called `recordTurn` once, leaving the employee at `'candidate'` (auto-onboards to `'active'` only after 3 turns, and the quit-roll gate requires `'active'`) — no quit ever rolled. Second attempt drove enough turns but the persisted file still showed `'candidate'` because `persist()` is throttled and my calls landed inside one throttle window — fixed by setting `status:'active'` explicitly in the direct file-state edit rather than depending on persist-timing. Did NOT wire the employee-quit Bark class (explicit KICKOFF out-of-scope). Gate: check-types/lint clean, server 538/538 (+1), webview/poller unchanged. Committed as 5e26214. Context is running low this session (~80% used) — next iteration should resume with items 7 (Bark payload fix), 8 (help/discoverability), 9 (FOCUS/osascript), 10-13 (F1-F4), then the final gate + handoff.
- 2026-07-09T14:07:00Z — item 7 DONE, implemented directly by the orchestrator. `notifyBark.ts` POSTed raw `text/plain`; the NEXUS Bark wrapper needs JSON `{task,status,message}` (422'd otherwise, per TUNING.md's live 2026-07-09 test). Fixed: `task="War Room"` constant, `status` mapped per class (`BIG_MOMENT_STATUS`: contract-completed→info, employee-quit/budget-paused/stop-all→warning, chain-failed→failure; morning digest→info), `message`=the push text. Masked-URL logging + fire-and-forget/no-crash posture unchanged. New tests assert the ACTUAL JSON body per class (not just that fetch fired) — 2 new tests, all 11 in the file pass. Full gate: server 540/540 (+2), check-types/lint clean. Committed 3688f91. This item's OTHER PASS criterion — one real push through the deployed NEXUS instance with a 200 in docker logs — is still gated on an actual deploy succeeding, which needs Deploy Gate 2/3 to clear the same harness permission wall already logged; not achievable this session, noted honestly rather than claimed. Next: item 8 (help/discoverability).
- 2026-07-09T14:30:00Z — item 8 DONE, built by a BUILD sub-agent, reporting here directly (orchestrator's own independent re-verification still pending). Baseline confirmed first: check-types/lint clean, server 540/540, webview 282/282, poller 85/85 (matched item-7 post counts exactly). Shipped all 3 pieces, no budget-cut fallback needed. (1) `ControlTooltip.tsx` (new) — hover+keyboard-focus popup, plain React state + CSS, no dependency; deliberately NOT named/merged with `Tooltip.tsx` (that's a dismissable first-run announcement card used by item 4's HudStack, unrelated concept — left untouched, no call sites changed). Wired onto the 10 controls the KICKOFF prompt's own candidate list named: Call/Chains/Orders/Layout/Settings/Help (BottomToolbar.tsx), the AgentDrawer kill button, the Economy HUD chip (EconomyHUD.tsx), a TRIAGE row (TriagePanel.tsx), and the zoom controls (ZoomControls.tsx) — 10 total, not the STOP-ALL button (KICKOFF's own list gave "kill control" and "crisis cards" as the two most literal reads, StopAllControl left as-is). Two real layout risks found and fixed while wiring, not just following the naive pattern: (a) EconomyHUD's outer div is deliberately `pointer-events-none!` (click-through, TUNING.md/HudStack's own documented anti-regression for a real historical bug) — wrapping the WHOLE chip in a naive tooltip span would have silently re-broken that, so `ControlTooltip` gained a scoped `className`/`focusable`/`testId` prop set and only the inner Cash+Reputation content group (not the outer chip) is wrapped, with `pointer-events-auto` applied to just that inner span; (b) TriagePanel's `.triage-row` is a full-width flex row inside a flex-column list — wrapping it in the tooltip's default `inline-flex` wrapper would have shrunk it to content width, so `ControlTooltip` gained a `display: 'inline-flex' | 'flex'` prop (never emits two conflicting Tailwind display utilities at once) and the row wrap uses `display=\"flex\" className=\"w-full\"`. TriagePanel's row also gained `role=\"button\" tabIndex={0}` + Enter/Space keydown (it was a plain onClick div with no native focusability, so \"keyboard focus\" couldn't have reached it otherwise). (2) `rotatingTips.ts` (new) — 20 short one-line tips grounded in real shipped mechanics (crisis aging, STOP ALL, KILL, standing orders, streaks, LEAN grading, etc.), `RotatingTip.tsx` cycles one every 8s, mounted in the bottom-right HudStack next to VersionIndicator. `testHooks.ts` gained `getRotatingTips()` so e2e asserts the displayed tip against the REAL array (not a hardcoded string duplicated in the test). (3) `HelpModal.tsx` restructured into a categorized quick-menu: `helpContent.ts`'s 15 existing sections each gained a `category` field (6 categories: SIGNALS/DISPATCH/PROGRESS & ECONOMY/OFFICE LIFE/BRIEFING/SOUND) — pure organizational metadata, zero new prose added to the content itself per KICKOFF's explicit \"don't add more prose\" instruction. Categories render as collapsible headers (default collapsed, skimmable at a glance), expanding to show the unmodified existing sections — directly replaces the old \"wall of text\" (all 15 sections always fully expanded). New `e2e/tests/standalone/help-discoverability.spec.ts` (2/2 PASS): test 1 (desktop) drives all 10 tooltips through BOTH `.hover()` and `.focus()`/`.blur()`, asserting the popup appears and then genuinely hides each time (not just appears once) — including the 2 controls needing real session setup (kill: registered runner + machine/pid-tagged hook session, same pattern as `kill.spec.ts`; triage row: mocked permission-denied crisis, same pattern as `triage.spec.ts`), then opens the quick-menu and asserts all 6 category testids render + expanding one reveals its sections, then asserts the rotating tip's displayed text is a real member of `getRotatingTips()`. Test 2 (mobile, iPhone-14 390×844) re-verifies the quick-menu + rotating tip + one representative tooltip at that viewport. Screenshots + grayscale copies for both viewports in `test-results/e2e/help-discoverability-*.png` (toolbar-tooltip, triage-tooltip, quick-menu, rotating-tip ×2 viewports), all visually verified directly (legible shape+word, categorized menu renders cleanly at both sizes, no horizontal overflow at 390px). Hit and fixed 3 real bugs while getting the test green, not product bugs but real test/harness gaps worth recording: (a) the e2e harness serves a PRE-BUILT `dist/` bundle, not live source — a stale build silently made the FIRST test run exercise pre-edit code (still showing old `title=` attributes, no ControlTooltip wrapper) with a misleadingly generic \"element not visible\" failure; fixed by rebuilding (`npm run build:webview` + `node esbuild.js`) before every e2e run from here on; (b) my own mobile-viewport test initially destructured `{ page }` only, omitting the `standalone` fixture — since Playwright fixtures are resolved lazily by destructuring, this meant `launchStandalone` never ran and the page stayed blank (a body length of 0), NOT a real app failure; (c) Escape does NOT close AgentDrawer (App.tsx's global keydown handler only toggles `isHelpOpen` — the drawer closes via its own \"x\" button or backdrop click), so a naive `page.keyboard.press('Escape')` between drawer interactions left the modal backdrop up, silently swallowing every subsequent hover for the rest of the test; fixed by clicking the drawer's own scoped close button instead. Full gate: check-types clean, lint clean (one `simple-import-sort` violation on the new `ControlTooltip.tsx`, autofixed + prettier-formatted), server 540/540, webview 282/282, poller 85/85 — all unchanged, confirming this item touched webview-only surface with zero server/poller blast radius. Full `npm run build` clean (PWA output present, 11 precache entries). Committed as bc186a3 (13 files: 4 new — ControlTooltip.tsx, RotatingTip.tsx, rotatingTips.ts, help-discoverability.spec.ts — 9 modified). Nothing deferred to TUNING.md — all 3 pieces shipped inside the cap with no forced compromises. Orchestrator re-verification still outstanding. Next: item 9 (FOCUS/osascript).
- 2026-07-09T14:50:00Z — item 8 independently re-verified by the orchestrator: re-ran check-types/lint (clean) and full suite (server 540/540, webview 282/282, poller 85/85, exact unchanged match confirming webview-only blast radius). Rebuilt fresh and re-ran help-discoverability.spec.ts myself (2/2 pass, both desktop+mobile). Hit one apparent false-alarm during verification — an `ls`/`find` check for the screenshot files came back empty because it raced a background full-suite e2e run I'd just kicked off (which was mid-execution and hadn't reached that test yet); re-ran the specific test in isolation and confirmed all 12 screenshot files (6 states × 2 viewports/grayscale) exist. Viewed 2 directly (desktop quick-menu showing collapsible SIGNALS category expanded with real help content; desktop toolbar-tooltip screenshot showing the rotating TIP box legible bottom-right) — both correct, legible, no layout issues. Did not do a line-by-line diff read of all 13 files given context constraints this session, but the gate + e2e + visual evidence is consistent and convincing. No further findings. Item 8 confirmed done.
- 2026-07-09T15:05:00Z — item 9 settled as review-on-return (dispatched as a fresh task by the team lead; baseline re-confirmed first: check-types/lint clean, server 540/540, webview 282/282, poller 85/85, exact match). Diagnosed per KICKOFF's own instruction, real stderr captured before touching code: ran the actual `osascript -e 'tell application "System Events" to set frontmost of (first process whose unix id is <pid>) to true'` shape against a real running headless `claude` CLI pid (53345) AND a real running GUI app pid (Terminal.app, 4213) — both hung ~60s and returned the IDENTICAL `execution error: System Events got an error: AppleEvent timed out. (-1712)`. Root cause (a) (pid lacks GUI window identity) predicts the GUI target succeeds while the CLI target fails with a distinct error; it did not — ruling out (a) and confirming (b): the calling process has no Automation/TCC consent for System Events, so macOS queues a consent dialog nothing can dismiss, and the native AppleEvent IPC blocks until its own ~60s timeout. Traced why Greg's originally-reported error read as a bare `Command failed: osascript -e '...'` with no detail: `FOCUS_TIMEOUT_MS=5_000` (`bin/dispatch-runner.mjs:85`) kills the child via Node's exec timeout 12x faster than the native 60s AppleEvent timeout ever fires, so `shortErr` only ever sees the generic exec-kill message in production — same root cause, just explains the truncated text. Confirmed the real runner is launchd, not an interactive terminal: `~/Library/LaunchAgents/com.war-room.dispatch-runner.plist` runs the fnm-managed `node` binary with `RunAtLoad`+`KeepAlive`, no bound TTY — a LaunchAgent has no interactive surface to click through a first-run Automation consent dialog, so this cannot self-resolve no matter how `attemptFocus` is restructured. Per KICKOFF's explicit instruction for root cause (b): did NOT attempt a code fix. Logged a full REVIEW-ON-RETURN entry in TUNING.md ("[KICKOFF v1.1 item 9]") with the exact System Settings → Privacy & Security → Automation → `node` → System Events path, a `launchctl kickstart` nudge to force the first-run prompt to surface, and a copy-pasteable diagnostic Greg can run himself to confirm consent was granted. No test/gate changes (no code touched) — check-types/lint/full suite unchanged at 540/282/85. Committed TUNING.md + STATE-v1.1.md together as item 9's settling commit, separate from any items 10-13 work. Next: items 10-13 (F1-F4), Greg-confirmed order F4→F2→F1→F3.
